# Exact dependency versions for reproducible Nuitka builds.
#
# requirements.txt uses upper-bound ranges for day-to-day development so
# contributors and CI (test.yml) catch upstream breakage early; this file
# pins exact versions so the compiled .exe (build.yml) is reproducible
# across builds regardless of what's newly published on PyPI.
#
# Regenerate after bumping requirements.txt:
#   python -m venv .venv-lock
#   .venv-lock\Scripts\pip install -r requirements.txt
#   .venv-lock\Scripts\pip freeze > requirements-lock.txt
MouseInfo==0.1.3
Pillow==12.3.0
PyAutoGUI==0.9.54
PyGetWindow==0.0.9
PyMsgBox==2.0.1
pynput==1.8.2
pyperclip==1.11.0
PyRect==0.2.0
PyScreeze==1.0.1
pytweening==1.2.0
pywin32==312
six==1.17.0
